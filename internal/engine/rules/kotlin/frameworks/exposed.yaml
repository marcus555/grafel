frameworks:
  name: Exposed
  category: orm_sql
  gradle_coordinates:
  - org.jetbrains.exposed:exposed-core
  - org.jetbrains.exposed:exposed-dao
  - org.jetbrains.exposed:exposed-jdbc
  detection:
    config_files: []
    build_signals:
    - '"org.jetbrains.exposed:exposed-core"'
    - '"org.jetbrains.exposed:exposed-dao"'
    import_patterns:
    - import org.jetbrains.exposed.sql.
    - import org.jetbrains.exposed.dao.
    code_patterns:
    - 'object * : Table('
    - transaction {
    - SchemaUtils.create(
    - 'select { '
    - insert {
  notes: 'JetBrains ORM — DSL and DAO layers. transaction {} block is high-confidence.
    object declarations extending Table() define schema. No config files — pure Kotlin
    code API.

    '
