import { defineConfig } from 'astro/config';

// Static output (default). The landing page is a self-contained static site;
// the build emits plain HTML/CSS/JS into dist/.
export default defineConfig({});
