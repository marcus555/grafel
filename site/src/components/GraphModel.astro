<section class="alt">
  <div class="wrap">
    <div class="section-head reveal">
      <p class="eyebrow">The graph model</p>
      <h2 class="section-title">59 entity kinds. 106 relationship kinds.</h2>
      <p class="section-sub">Extraction turns source into a graph: nodes are the things in your system, edges are how they relate — the same shape whether it's a function call or a Kafka topic.</p>
    </div>
    <div class="legend reveal">
      <div class="col">
        <h3>Entities <span class="col-count">· 59</span></h3>
        <p class="cap">The nodes — what exists in the system.</p>
        <div id="entkinds"></div>
      </div>
      <div class="col">
        <h3>Relationships <span class="col-count">· 106</span></h3>
        <p class="cap">The typed edges — how nodes connect.</p>
        <div id="relkinds"></div>
      </div>
    </div>
    <p class="legend-note reveal">The complete set, generated from the type registry.</p>
  </div>
</section>
