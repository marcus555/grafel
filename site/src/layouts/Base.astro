---
// Global (unscoped) stylesheets — order matters: tokens first, then everything else.
// These are the byte-preserved extraction of the original inline <style> block,
// split only by concern. They stay global so the cascade is identical.
import '../styles/tokens.css';
import '../styles/global.css';
---
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>grafel — map your codebase, navigate any part</title>
<style>html,body{margin:0;padding:0}</style>
</head>
<body>
<slot />
</body>
</html>
